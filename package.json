{
  "name": "contextapi",
  "version": "1.0.0",
  "description": "Web pages, LLM-ready in one call",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@upstash/redis": "^1.34.0",

    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}